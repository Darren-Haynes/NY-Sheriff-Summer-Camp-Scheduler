import { useMediaQuery } from "react-responsive";

export default function Header() {
  const summerCampText = useMediaQuery({ query: "(min-width: 750px)" });

  const summerCampStyles = {
    marginTop: summerCampText ? "22px" : "8px",
    marginLeft: summerCampText ? "50px": "0px"
  };
  return (
    <header>
      <div id="navbar">
        <div className="navbar-content">
          <h1 id="navbar-left" style={summerCampStyles}>
            Summer Camp
          </h1>
        </div>
        <div className="navbar-content">
          <h1 id="navbar-right">Scheduler</h1>
        </div>
      </div>
    </header>
  );
}
